{
  "name": "@git-lfs-hub/ci-cd",
  "type": "module",
  "private": true,
  "scripts": {
    "compile": "tsgo --noEmit",
    "test": "bun run compile && bun --bun vitest run",
    "test-coverage": "bun run test --coverage"
  },
  "packageManager": "bun@1.3.13",
  "devDependencies": {
    "@types/bun": "latest",
    "@typescript/native-preview": "^7.0.0-dev.20260602.1",
    "@vitest/coverage-istanbul": "^4.1.7",
    "vitest": "^4.1.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
