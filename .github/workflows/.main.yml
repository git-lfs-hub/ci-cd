name: Main

on:
  pull_request:
  push: { branches: [main] }
  workflow_dispatch:

permissions: {}

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: raven-actions/actionlint@v2
      - run: "bun run test"
