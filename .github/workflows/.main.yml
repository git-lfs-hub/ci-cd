name: Main

on:
  push: { branches: [main] }
  pull_request:
  workflow_dispatch:

permissions: {}

jobs:
  actionlint:
    name: Actions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: raven-actions/actionlint@v2
